
#include <stdio.h>

int main()
{
    int a;
    printf("Enter the numbner:");
    scanf("%d",&a);
    if(a&&1==0)
    printf("the number %d is even number:",a);
    else
    printf("the number %d is not even number:",a);

    return 0;
}
